events {
    worker_connections 1024;
}

http {
    upstream frontend {
        server frontend;
    }

    upstream backend {
        server backend;
    }

    server {
        listen 80;

        location /api/ {
            proxy_pass http://backend;
        }

        location / {
            proxy_pass http://frontend;
        }
    }
}
